<div class="topic-new-wrapper" >
  <div class="container">
    <div class="row">
<!--Bootstrapにおいて.rowは左右両端の余分なガターを
ネガティブマージン（CSSにおいて、マイナスの値を入力することで
コンテンツエリアを拡大させること）で相殺する役割を持っている。-->
      <div class="col-md-6 col-md-offset-3">
        <h1 class="text-center">Add Picture</h1>
        <%= form_for @topic do |f| %>
        <!--form_for=フォームを作成する際に最もよく使うメソッド
        form_forの引数は、モデルクラスのインスタンス変数-->
          <div class="form-group">
            <%= f.label :image %>
            <%= f.file_field :image%>
          </div>

          <div class="form-group">
            <%= f.label :description %>
            <%= f.text_area :description, class: 'form-control' %>
          </div>
          
          <% if current_user != nil %>
              <%= f.submit '投稿', class: 'btn btn-black btn-block' %>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>